# Node image for building React + Vite
image: node:20.11.1-bullseye

# Install dependencies and build
command: npm ci && npm run build

# Deploy Vite production output
public: dist

# SPA fallback - serve index.html for all routes
spa: true
