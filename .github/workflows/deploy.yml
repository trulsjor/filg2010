name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Statichost build
        run: curl -X POST https://builder.statichost.eu/fjellhammeril-g2010
